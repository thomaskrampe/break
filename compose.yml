services:
  caddy:
    image: caddy/caddy:2.5.2-alpine
    # image: caddy/caddy:latest
    container_name: lunchbreak
    restart: unless-stopped
    # ports:
    #   - '80:8080'
    volumes:
      - ./public:/public
      - ./data:/data:ro
      - /etc/localtime:/etc/localtime:ro
    networks:
      - reverse-proxy
    command: caddy run --config ./data/Caddyfile
networks:
  reverse-proxy:
    external: true
